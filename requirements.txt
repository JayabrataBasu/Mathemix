# Legacy requirements placeholder
#
# Dependencies are now managed via `pyproject.toml` (see the [project]
# dependencies section). This file is kept to avoid breaking automation that
# still calls `pip install -r requirements.txt`; we redirect those callers to
# use maturin + the Python metadata instead.
